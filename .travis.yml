sudo: true

language: python

python:
   - "3.6"

dist: trusty

cache: pip

install:
   - pip install tox-travis
   - pip install codecov
   - pip install requirements.txt

script:
   - tox --version
   - tox
